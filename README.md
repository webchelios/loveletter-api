# loveletter-api
1. Instalar dependencias ```npm install```
2. Levantar proyecto ```npm start```

En caso de necesitar las variables de entorno: 
```
PORT=3000
CONNECT="mongodb://localhost:27017/movies"
SEED="secret"
EXPIRATION=1h
```